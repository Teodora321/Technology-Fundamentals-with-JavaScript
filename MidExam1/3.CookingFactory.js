function solve(batches) {
    input.pop();

    for(let batch of batches){
        let currentBatch = batch.split('#').map(Number);
        let isItTheBest = false;

    }
}

solve(['5#4#10#-2', '10#5#2#3#2', 'Bake It!'])